# Portfolio

Vite + React site for [adamsonb.com](https://adamsonb.com).

## Local

```bash
npm install
npm run dev
```

## Deploy (on server)

```bash
./deploy.sh
```

That pulls `main`, installs deps, and builds into `dist/`.
